template_id: baseline.settings.presence_only
display_name: Settings（设置页）
template_kind: baseline_skill
genre: universal
template_version: "1.0.0"
schema_version: "1.0.0"
evaluator_version: "1.0.0"

phase_scope:
  - phase1_local_multiplayer

description: |
  设置页 Baseline 模板。
  不可删减底线：Master Volume + SFX Volume + Window Mode + Resolution + Apply + Back。
  可 defer 项：Music Volume、Graphics Quality、Key Rebinding、持久化、Restore Defaults。
  持久化策略默认 session_only（session 内生效即可）。

realization_class: presence_only

input_contract_ref: root_skill_contract.baseline_capabilities.settings
output_schema_ref: output_schema.json

requires_static_vocab:
  - volume_slider
  - window_mode
  - resolution_option
  - settings_action

can_emit_families:
  - settings_spec

depends_on_templates: []

priority: medium

# Phase 13: 能力绑定(原 skill_graph_planning.BASELINE_NODE_CONFIGS 数据下沉,逐字照抄)
capability_bindings:
  - capability_id: baseline-settings
    instance_id: skill-baseline-settings
    convergence_priority: 5
    related_clarification_items:
      - cg-platform-persistence
      - cg-platform-foundation-boundary
    planning_notes:
      - "Settings 必须保留六项最低控件，但不在本阶段决定持久化实现细节。"
    fragment_family: settings_spec
